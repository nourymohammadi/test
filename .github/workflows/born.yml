name: Born Workflow
on: workflow_dispatch
jobs:
  start-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world!"
      - name: Finished
        run: echo "I'm done"
